import { FiltroAseguradoraPipe } from './filtro-aseguradora.pipe';

describe('FiltroAseguradoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAseguradoraPipe();
    expect(pipe).toBeTruthy();
  });
});
