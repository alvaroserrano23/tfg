import { FiltroEspecialidadPipe } from './filtro-especialidad.pipe';

describe('FiltroEspecialidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEspecialidadPipe();
    expect(pipe).toBeTruthy();
  });
});
