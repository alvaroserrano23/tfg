import { FiltroComunidadPipe } from './filtro-comunidad.pipe';

describe('FiltroComunidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroComunidadPipe();
    expect(pipe).toBeTruthy();
  });
});
