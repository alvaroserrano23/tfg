import { FiltroLocalidadPipe } from './filtro-localidad.pipe';

describe('FiltroLocalidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLocalidadPipe();
    expect(pipe).toBeTruthy();
  });
});
