import { FiltroProvinciaPipe } from './filtro-provincia.pipe';

describe('FiltroProvinciaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProvinciaPipe();
    expect(pipe).toBeTruthy();
  });
});
