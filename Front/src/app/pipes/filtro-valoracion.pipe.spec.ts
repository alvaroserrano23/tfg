import { FiltroValoracionPipe } from './filtro-valoracion.pipe';

describe('FiltroValoracionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroValoracionPipe();
    expect(pipe).toBeTruthy();
  });
});
